import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Shop All | Stride",
    description: "Browse our collection of premium footwear",
};

export { default } from "@/Modules/Products";
