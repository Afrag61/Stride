import NotFound from "./NotFound";

export const metadata = {
    title: "Stride | Not Found",
    description: "Page not found",
};
const Index = () => {
    return <NotFound />;
};

export default Index;
